---
sidebar_position: 5
sidebar_label: Sync
---
import React from 'react';

# Sync

export const Algo = ({ lines }) => (
  <div style={{
    background: 'var(--lasco-surface-alt)',
    fontFamily: 'var(--ifm-font-family-monospace)',
    fontSize: 'var(--ifm-code-font-size)',
    lineHeight: 1.6,
    overflowX: 'auto',
    padding: '1rem 0',
    margin: '0 0 1.25rem',
    borderRadius: 'var(--ifm-pre-border-radius)',
    boxShadow: 'var(--ifm-global-shadow-lw)',
  }}>
    <div style={{ minWidth: 'max-content' }}>
      {lines.map(([indent, text], i) => (
        <div key={i} style={{
          display: 'flex',
          alignItems: 'center',
          paddingLeft: `calc(1rem + ${indent * 20}px)`,
          paddingRight: '1rem',
        }}>
          <span style={{
            whiteSpace: 'pre',
            color: text.trimStart().startsWith('//') ? 'var(--lasco-ink-muted)' : 'var(--lasco-ink)',
          }}>{text || ' '}</span>
        </div>
      ))}
    </div>
  </div>
);

export const AccessCard = ({ children }) => (
  <div style={{
    border: '2px solid var(--lasco-line)',
    padding: '16px 20px',
    margin: '0 0 1.25rem',
    background: 'var(--lasco-surface-alt)',
  }}>
    {children}
  </div>
);

## Fetch

### Summary

Fetch lists and downloads remote operation files missing from the remote's last-known state, verifies that the remote has not lost any previously known operation groups, then merges unseen operations into the local log and rebuilds in-memory state. It only adds missing `mk_*.enc` files and does not download media or thumbnails. Fetch never modifies or removes a remote file.

### Required access

<AccessCard>

- **Sync policy:** Acquire the per-remote sync slot and the global fetch slot.
- **Remote:** List and read access. <span className="tag tag-ok">Safe because of per-remote sync slot</span>
- **Local:**
  - Read and append to the local operation log. <span className="tag tag-warn">Safety to be determined</span>
  - Read and atomically write the remote's last-known state. <span className="tag tag-ok">Safe because of per-remote sync slot</span>
  - Atomically write missing `mk_*.enc` files in the library directory. <span className="tag tag-ok">Safe because of global fetch slot</span>
- **In-memory local state:** Rebuild from the local operation log. <span className="tag tag-ok">Safe under mutex</span>

</AccessCard>

### Behavior

<table className="behavior-table" style={{ width: '100%', tableLayout: 'fixed' }}>
  <thead>
    <tr>
      <th style={{ width: '1%', whiteSpace: 'nowrap' }}>#</th>
      <th>Behavior</th>
      <th>Required authorization</th>
      <th>Interruption safety</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{ whiteSpace: 'nowrap' }}>1</td>
      <td>Download each `mk_*.enc` file present on the remote but missing from the local library. Never remove or replace an existing local master-key file.</td>
      <td><span className="tag tag-ok">List remote</span> <span className="tag tag-ok">Read remote</span> <span className="tag tag-warn">Write local key file</span></td>
      <td>Atomic `mk_*.enc` writes.</td>
    </tr>
    <tr>
      <td style={{ whiteSpace: 'nowrap' }}>2</td>
      <td>Identify the remote operation files that are not already recorded in the last-known state for the remote.</td>
      <td><span className="tag tag-ok">List remote</span></td>
      <td></td>
    </tr>
    <tr>
      <td style={{ whiteSpace: 'nowrap' }}>3</td>
      <td>Download and validate those files without changing the last-known state for the remote.</td>
      <td><span className="tag tag-ok">Read remote</span></td>
      <td></td>
    </tr>
    <tr>
      <td style={{ whiteSpace: 'nowrap' }}>4</td>
      <td>Verify that the resulting set of remote operation-group IDs is a superset of the group IDs previously known from this remote. If it is not, fail without writing anything.</td>
      <td><span className="tag tag-ok">Read local state</span></td>
      <td></td>
    </tr>
    <tr>
      <td style={{ whiteSpace: 'nowrap' }}>5</td>
      <td>Atomically write the validated files to the last-known state for the remote.</td>
      <td><span className="tag tag-warn">Write local state</span></td>
      <td>Atomic write. If terminated, the last-known state contains a subset of the current remote state.</td>
    </tr>
    <tr>
      <td style={{ whiteSpace: 'nowrap' }}>6</td>
      <td>Append operation groups not already in the local log, then rebuild in-memory local state.</td>
      <td><span className="tag tag-warn">Append local log</span> <span className="tag tag-warn">Rebuild in-memory state</span></td>
      <td>TODO: Think about the implications of an up-to-date last-known state that is not appended to the local log.</td>
    </tr>
  </tbody>
</table>

### Interruption safety

- Before a temporary file is renamed, an interruption leaves the previous last-known state for the remote unchanged. That state records only fully downloaded and validated operation files.
- During a temporary-file write, an incomplete temporary file may remain and can be discarded.
- After a file is renamed into place but before last-known-state metadata is updated, an extra local file may remain. The last-known state may therefore be stale, but it never records an incomplete or unvalidated download as known.
- During a log append, previously written frames remain valid. The operation log contains only complete frames, so a partial final frame is ignored on the next read.
- Master-key files are additive: an interruption may leave a temporary file, but fetch never removes or replaces an existing local `mk_*.enc` file.
- In-memory local state is not durable. If fetch terminates during its rebuild, the next rebuild starts from the persisted operation log.

## Push

Push uploads all files that are considered missing on the target remote, based on its last known state.

For operation files, the client simply copies what it has locally. For media and thumbnail files, since they are downloaded lazily, the client may not have them on disk. Because the last known state of all remotes is stored locally, the push procedure can download the needed files directly from another remote and immediately upload them to the target, without performing a full fetch.

### Algorithm

**1. Sync library directory (mk files)**

<Algo lines={[
  [0, "remote_library_files = list all files in library/ on the remote"],
  [0, "remote_uuid = parse UUID from the library_id_{uuid} filename in remote_library_files"],
  [0, "assert remote_uuid == local library_id   // error if mismatch"],
  [0, "local_mk_files  = files matching mk_*.enc in local_state/library/"],
  [0, "remote_mk_files = files matching library/mk_*.enc on the remote"],
  [0, ""],
  [0, "for each file in local_mk_files not in remote_mk_files:"],
  [1, "upload from local_state/library/ to library/ on the remote"],
]} />

**2. Push operations**

<Algo lines={[
  [0, "if pending.op exists:"],
  [1, "append it to operations.log"],
  [1, "delete pending.op"],
  [0, ""],
  [0, "// determine which op groups need to be uploaded"],
  [0, "// by comparing the local log against the last known remote state"],
  [0, "ops_to_upload = op groups in operations.log not in last known remote state"],
  [0, ""],
  [0, "compacted_files = compact(ops_to_upload)"],
  [0, "upload each file in compacted_files to the remote"],
  [0, "apply operation compaction if needed   // see Compaction in Operations"],
  [0, ""],
  [0, "update last known state: record every file uploaded or produced by"],
  [0, "compaction in remotes/{remote_id}/state/operations/, and remove entries"],
  [0, "for any files that compaction deleted on the remote"],
]} />

**3. Push media and thumbnails**

<Algo lines={[
  [0, "remote_media_list = remotes/{remote_id}/state/media/media_list.json"],
  [0, ""],
  [0, "for each media_id in reconstructed state not in remote_media_list:"],
  [1, "if local_state/media/YYYY/MM/{media_id}.data exists on disk:"],
  [2, "upload the data blob"],
  [2, "same for thumbnail"],
  [2, "add media_id to remotes/{remote_id}/state/media/media_list.json"],
]} />

### Duplicate operations on the remote

With concurrent clients pushing to the same remote, or a single client pushing to several remotes, the same operation group can end up uploaded more than once. This is rare in practice. An op group that appears more than once is applied only once to the local log. We do not attempt to detect or remove duplicates on the remote.
